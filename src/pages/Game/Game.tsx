import React from 'react';
import { GameHeader, GameForm, CardsList } from '../../components';

function game() {
  return (
    <div>
      <GameHeader />
      <GameForm />
      <CardsList />
    </div>
  );
}

export default game;
